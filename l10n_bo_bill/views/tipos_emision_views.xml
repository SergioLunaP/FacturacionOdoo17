<odoo>
    <!-- Vista Formulario -->
    <record id="view_tipos_emision_form" model="ir.ui.view">
        <field name="name">tipos.emision.form</field>
        <field name="model">l10n_bo_bill.tipos_emision</field>
        <field name="arch" type="xml">
            <form string="Tipos de Emisión">
                <sheet>
                    <group>
                        <field name="external_id" invisible="1"/>
                        <field name="codigo_clasificador"/>
                        <field name="descripcion"/>
                        <field name="codigo_tipo_parametro"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Vista Tree -->
    <record id="view_tipos_emision_tree" model="ir.ui.view">
        <field name="name">tipos.emision.tree</field>
        <field name="model">l10n_bo_bill.tipos_emision</field>
        <field name="arch" type="xml">
            <tree string="Tipos de Emisión">
                <field name="codigo_clasificador"/>
                <field name="descripcion"/>
                <field name="codigo_tipo_parametro"/>
            </tree>
        </field>
    </record>

    <!-- Acción para abrir la vista -->
    <record id="action_tipos_emision" model="ir.actions.act_window">
        <field name="name">Tipos de Emisión</field>
        <field name="res_model">l10n_bo_bill.tipos_emision</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Acción del servidor para la sincronización -->
    <record id="action_tipos_emision_sync" model="ir.actions.server">
        <field name="name">Sincronizar Tipos de Emisión</field>
        <field name="model_id" ref="model_l10n_bo_bill_tipos_emision"/>
        <field name="state">code</field>
        <field name="code">model.obtener_tipos_emision_desde_api()</field>
    </record>
</odoo>
